node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
node_modules/.tmp

# Claude Code local settings
.claude/settings.local.json

# UiPath Coded Apps deploy artifacts — all regenerated by `uip codedapp pack`
# and `publish`. Holds the .nupkg plus per-deployment registration metadata.
# (CLI credentials live in ~/.uipath/.auth, not here.)
.uipath/
