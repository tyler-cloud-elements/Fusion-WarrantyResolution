# @uipath/uipath-typescript ships on public npm. Pinned explicitly so the scope
# never falls through to a private/gated feed inherited from a parent .npmrc.
@uipath:registry=https://registry.npmjs.org/
